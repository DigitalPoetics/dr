---
// Props: id (string), content (string)
const { id, content } = Astro.props;
---
<p id={`footnote-${id}`}>
  <sup>{id}</sup> <span set:html={content} />
  <a href={`#ref-${id}`} aria-label="Back to reference">↩</a>
</p>

<style>
  p {
    font-size: 1rem;
    margin: 0.5rem 0 0 0;
  }

</style>
