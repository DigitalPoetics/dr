---
import BaseLayout from "../layouts/BaseLayout.astro";
import SearchPage from "../components/fuse/SearchPage.astro";
// import { SITE_DESCRIPTION } from "../consts";

import SearchInput from "../components/fuse/SearchInput.astro";
import SearchResults from "../components/fuse/SearchResults.astro";
import deleuzeLectures from "../data/deleuzian/the-deleuze-seminars.json";

// const prerender = false;
---

<BaseLayout 
  pageTitle="Search the Blog" 
  description={"Enter a search term or phrase to search the blog."}
>
  <!-- <main> -->
    <!-- <SearchPage 
      searchData={deleuzeLectures}
      label="Deleuzian: "
      description="Search through Gilles Deleuze lectures by title, content, or author."
      placeholder="Search the Deleuze Seminars"
      searchTitle="Deleuze Lecture Search"
      searchKeys={[
        { name: "title", weight: 2 },
        { name: "text", weight: 1.5 },
        { name: "author", weight: 1 },
        { name: "number", weight: 0.5 }
      ]}
      linkPrefix=""
      authorField="author"
      titleField="title" 
      dateField="date"
      slugField="url"
    /> -->

    <main>
      <!-- Page search input - updates URL params -->
      <SearchInput 
        label="Search Deleuze Lectures"
        description="Search through Gilles Deleuze lectures by title, content, or author."
        placeholder="Search lectures..."
      />
      
      <!-- Results display - reads from URL params -->
      <SearchResults 
        searchData={deleuzeLectures}
        searchTitle="Deleuze Lecture Search"
        searchKeys={[
          { name: "title", weight: 2 },
          { name: "text", weight: 1.5 },
          { name: "author", weight: 1 },
          { name: "number", weight: 0.5 }
        ]}
        linkPrefix=""
        authorField="author"
        titleField="title" 
        dateField="date"
        slugField="url"
      />
    </main>
  <!-- </main> -->


</BaseLayout>